# 📋 СТАНДАРТИЗОВАНІ ПАРАМЕТРИ НАВЧАННЯ

## Для чесного порівняння моделей

Всі моделі тренуються з **ОДНАКОВИМИ** параметрами:

---

## 🎯 ФІКСОВАНІ ПАРАМЕТРИ (однакові для всіх)

```python
# Dataset
DATASET = "data/sudoku.csv"
TRAIN_SPLIT = 0.8
LIMIT = None  # Весь датасет (1M+)

# Training
EPOCHS = 40  # Однакова кількість епох для всіх
BATCH_SIZE = 64  # Однаковий batch size
LEARNING_RATE = 0.001  # Однаковий початковий LR
WEIGHT_DECAY = 1e-5  # Однакова регуляризація

# Optimizer
OPTIMIZER = "Adam"

# Scheduler
SCHEDULER = "ReduceLROnPlateau"
SCHEDULER_PATIENCE = 3
SCHEDULER_FACTOR = 0.5

# Device
DEVICE = "cuda" if available else "cpu"

# Other
NUM_WORKERS = 0
SAVE_DIR = "weights"
```

---

## 🔧 МОДЕЛЬ-СПЕЦИФІЧНІ ПАРАМЕТРИ

Ці параметри визначають архітектуру, але не впливають на справедливість:

### CNN Baseline
```python
--model baseline
--hidden-channels 64  # архітектура моделі
```

### CNN Advanced
```python
--model advanced
--hidden-channels 128  # архітектура моделі
--num-residual-blocks 20  # архітектура моделі
```

### GNN
```python
--model gnn
--hidden-channels 128  # архітектура моделі
--num-gnn-layers 8  # архітектура моделі
```

### RNN
```python
--model rnn
# embedding_dim=64, hidden_size=128, num_layers=2
# (задані в models/rnn_model.py)
```

---

## 📊 КОМАНДИ ДЛЯ НАВЧАННЯ (стандартизовані)

### CNN Baseline
```bash
python train.py \
    --model baseline \
    --epochs 40 \
    --batch-size 64 \
    --lr 0.001 \
    --hidden-channels 64 \
    --device cuda \
    --weight-decay 1e-5 \
    --save-dir weights
```

### CNN Advanced
```bash
python train.py \
    --model advanced \
    --epochs 40 \
    --batch-size 64 \
    --lr 0.001 \
    --hidden-channels 128 \
    --num-residual-blocks 20 \
    --device cuda \
    --weight-decay 1e-5 \
    --save-dir weights
```

### GNN
```bash
python train.py \
    --model gnn \
    --epochs 40 \
    --batch-size 64 \
    --lr 0.001 \
    --hidden-channels 128 \
    --num-gnn-layers 8 \
    --device cuda \
    --weight-decay 1e-5 \
    --save-dir weights
```

### RNN
```bash
python train.py \
    --model rnn \
    --epochs 40 \
    --batch-size 64 \
    --lr 0.001 \
    --device cuda \
    --weight-decay 1e-5 \
    --save-dir weights
```

---

## ✅ Що ОДНАКОВЕ (чесне порівняння):

- ✅ Датасет: `data/sudoku.csv` (весь)
- ✅ Train/Val split: 80/20
- ✅ Epochs: 40
- ✅ Batch size: 64
- ✅ Learning rate: 0.001
- ✅ Optimizer: Adam
- ✅ Weight decay: 1e-5
- ✅ Scheduler: ReduceLROnPlateau (patience=3, factor=0.5)
- ✅ Gradient clipping: max_norm=1.0 (в train.py для всіх)
- ✅ Device: CUDA (якщо доступно)
- ✅ Random seed: (можна додати для повторюваності)

---

## 🔍 Що РІЗНЕ (архітектурні особливості):

- 🔧 Кількість параметрів (природна різниця архітектур)
- 🔧 Hidden channels (64 для baseline, 128 для інших)
- 🔧 Кількість шарів (5 для baseline, 20 для advanced, 8 для GNN, 2 для RNN)

**Це ОК!** Це природні відмінності архітектур, а не нечесні умови.

---

## 📝 ПРИМІТКИ:

1. **Batch size 64** - це компроміс:
   - Для baseline це може бути більше (128)
   - Для GNN це може бути менше (32)
   - Але 64 дозволяє всім моделям тренуватися стабільно

2. **40 epochs** - достатньо для всіх моделей:
   - Baseline конвергує за ~20-25 epochs
   - Advanced конвергує за ~30-35 epochs
   - GNN конвергує за ~35-40 epochs
   - RNN конвергує за ~25-30 epochs

3. **Gradient clipping** вже є в `train.py` (рядок 100):
   ```python
   torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)
   ```
   Це застосовується до ВСІХ моделей автоматично.

---

## 🎯 РЕЗУЛЬТАТ:

Після навчання всіх 4 моделей з цими параметрами ви отримаєте **чесне порівняння**, де різниця в accuracy буде обумовлена ТІЛЬКИ архітектурою моделі, а не параметрами навчання!

---

## 📊 Очікувані результати (з однаковими умовами):

| Модель | Cell Accuracy | Board Accuracy | Причина |
|--------|---------------|----------------|---------|
| **CNN Baseline** | ~85-88% | ~30-35% | Обмежений receptive field |
| **CNN Advanced** | ~92-94% | ~60-70% | Глибока мережа, skip connections |
| **GNN** | ~93-95% | ~65-75% | Графова структура, attention |
| **RNN** | ~80-85% | ~25-30% | Втрата 2D структури |

Різниця в accuracy тепер показує **реальну різницю архітектур**!
