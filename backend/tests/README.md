# 🧪 Tests

## Smoke Tests для Sudoku моделей

Цей файл містить базові тести для перевірки працездатності всіх моделей.

### Що тестується:

1. **Baseline CNN** - ініціалізація, forward pass, predictions
2. **Advanced CNN** - ініціалізація, forward pass, predictions
3. **RNN (LSTM)** - ініціалізація, forward pass, predictions
4. **GNN** - ініціалізація, forward pass, predictions
5. **Checkpoint Save/Load** - збереження/завантаження scheduler + RNG states
6. **Dropout Parameters** - всі моделі приймають dropout параметр

### Запуск:

```bash
# З кореня проекту
python backend/tests/test_models.py

# Або з папки backend
cd backend
python tests/test_models.py
```

### Очікуваний результат:

```
============================================================
TEST SUMMARY
============================================================
[OK] Passed: 6
[FAILED] Failed: 0
[SKIP] Skipped: 0
[TOTAL] Total: 6

[SUCCESS] ALL TESTS PASSED!
```

### Примітки:

- GNN тест автоматично пропускається якщо PyTorch Geometric не встановлено
- Тести використовують малі моделі для швидкості
- Checkpoint тести використовують тимчасові файли (автоматично видаляються)

### Вимоги:

- Python 3.8+
- PyTorch 2.0+
- NumPy
- PyTorch Geometric (опційно, для GNN тесту)
